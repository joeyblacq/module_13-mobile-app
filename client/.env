# STARTING NGROK: ngrok http 8080

# Everytime you restart ngrok, the URL above will change. Update it here accordingly.
EXPO_PUBLIC_NGROK_URL="https://divulsive-rebbeca-asterismal.ngrok-free.dev"

# When doing fetch() calls in the app, use the following base URL
# Example: fetch(`${process.env.EXPO_PUBLIC_NGROK_URL}/api/some-endpoint`)